{
  "name": "roboit",
  "version": "0.0.1",
  "type": "commonjs",
  "description": "This repository contains tools and scripts needed by IT organizations that are using different fleet management tools.",
  "author": {
    "name": "Robocorp",
    "email": "dev@robocorp.com",
    "url": "https://robocorp.com"
  },
  "license": "SEE LICENSE IN LICENSE",
  "main": "index.js",
  "bin": {
    "roboit": "./lib/src/cli.js"
  },
  "pkg": {
    "scripts": "./lib/**/*.js",
    "outputPath": "release",
    "assets": [
      "./lib/powershell/**/*",
      "./lib/bash/**/*",
      "./node_modules/figlet/fonts/3D-ASCII.flf"
    ]
  },
  "scripts": {
    "clean": "yarn rimraf lib dist release",
    "build": "npm-run-all clean build:app build:assets",
    "build:app": "npx tsc && npx tsc-alias",
    "build:assets": "cp -R ./package.json ./powershell ./bash ./lib/",
    "test": "yarn run test:types && jest --runInBand",
    "test:lint": "eslint --ext .tsx,.ts --max-warnings 0 .",
    "test:types": "tsc --noEmit",
    "test:prettier": "prettier --check src/",
    "fix": "npm-run-all fix:prettier fix:lint",
    "fix:prettier": "prettier --write src/ ",
    "fix:lint": "eslint --fix --ext .tsx,.ts .",
    "dev": "NODE_ENV=development npx ts-node src/cli.ts",
    "exe": "yarn clean && yarn build && yarn pkg .",
    "watch": "tsc --watch"
  },
  "engines": {
    "node": ">=18.17"
  },
  "engineStrict": true,
  "dependencies": {
    "@robocorp/dev-tools-commons": "^6.0.0",
    "chalk": "4.1.2",
    "easy-table": "^1.2.0",
    "figlet": "^1.7.0",
    "nanospinner": "^1.1.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/figlet": "^1.5.7",
    "@types/prompts": "^2.4.7",
    "copyfiles": "^2.4.1",
    "npm-run-all": "^4.1.5",
    "pkg": "^5.8.1",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.2.2"
  }
}
